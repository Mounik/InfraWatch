all:
  children:
    monitoring:
      hosts:
        monitor01:
          ansible_host: 127.0.0.1
          ansible_user: vagrant
          ansible_ssh_private_key_file: ~/.ssh/id_rsa

    agents:
      hosts:
        web01:
          ansible_host: 127.0.0.1
          ansible_user: vagrant
          labels:
            role: webserver
            env: testing