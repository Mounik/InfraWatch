---
# InfraWatch -- Deploy/redeploy agent only
# Usage: ansible-playbook -i inventory/hosts.yml playbooks/deploy-agent.yml --limit web01

- name: Deploy monitoring agent
  hosts: agents
  become: yes
  roles:
    - node-exporter
  vars:
    infrawatch_server: "{{ groups['monitoring'][0] | default(omit) }}"
